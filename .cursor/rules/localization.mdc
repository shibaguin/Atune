---
description: Локализация приложения
globs: 
---
1. Локализация через .resx файлы в `Resources/Localization/`
2. Для новых текстов добавляй записи в оба файла:
   - `ru.resx`
   - `en.resx`
3. Используй `DynamicResource` в XAML для доступа к локализованным строкам